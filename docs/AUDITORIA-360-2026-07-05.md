# Auditoria 360 — Sibanki (05/07/2026)

> Auditoria consolidada sob as 4 lentes do Conselho (Negócio, Produto,
> Engenharia, Segurança/Compliance). Foco: situação atual, governança,
> integrações e redução de entulho para não sobrecarregar a equipe.

---

## 1. Visão Executiva & Produto (TL;DR)

O produto está **em produção e saudável** — React SPA em `sibanki.com.br`,
52 Cloud Functions ativas, empacotamento mobile via Capacitor 8 no repo,
171 testes unitários + 35 Playwright. O gargalo não é o código: é
**sobrecarga de contexto e entulho documental**, além de higiene de git.
Esta sessão atacou justamente isso.

**O que foi resolvido nesta sessão (arquivos, sem commit):**

- CLAUDE.md **enxugado de 76 KB → 40 KB** (817 linhas). 473 linhas de logs
  de sessão datados (08/04 → 14/06) movidas para
  `docs/_arquivo/CLAUDE-logs-sessao-ate-2026-06.md` — nada apagado.
- **47 documentos históricos** (anteriores a maio/2026) movidos para
  `docs/_arquivo/`. Restam **35 docs vivos** em `docs/`.
- `.gitignore` reforçado com `public/index_files/` (JS do legado).
- Diff dos 26 arquivos não commitados revisado (ver seção 5).

> ⚠️ Nada foi commitado: o `.git/index.lock` está travado (o app desktop
> segura o índice). Os comandos git ficam para você rodar localmente (seção 6).

---

## 2. Situação Técnica & Governança

### Estrutura de governança — bem desenhada, mas era descumprida
Hierarquia correta e clara:
`AGENTS.md` (v2.1, fonte de verdade de engenharia) → `CLAUDE.md` (contexto
pinado) → `docs/CHANGELOG.md` (histórico incremental) →
`docs/INVENTARIO-COMPLETO-SISTEMA.md` (inventário detalhado).

**Furo corrigido:** a regra diz "histórico de sessão vai para o CHANGELOG,
não acumula no CLAUDE.md" — mas o CLAUDE.md vinha acumulando logs. Agora está
enxuto e respeitando a própria regra.

### Higiene de branch (ação sua)
- `main` com **26 arquivos modificados não commitados** — passe de contraste
  coeso (ver seção 5), commitável como uma peça.
- `.git/index.lock` órfão travando operações git — remover antes de commitar.
- 3 branches paralelas antigas para decidir merge/arquivo:
  - `chore/upgrade-firebase-v6-adminv13` (25/jun)
  - `feat/dashboard-cartoes-visual` (18/jun)
  - `fix/pwa-splash-screen` (23/jun)

### Entulho — antes → depois

| Item | Tamanho | Antes | Agora |
|---|---|---|---|
| `docs/` na raiz | 1.3 MB | 84 arquivos misturados | 35 vivos + `_arquivo/` |
| CLAUDE.md | — | 76 KB (com logs) | 40 KB (enxuto) |
| `screenshots/` (git) | 11 MB | tracked apesar do ignore | destrackar (seção 6) |
| `public/index_files/*.baixados` | ~1 MB | tracked, legado morto | ignorado + destrackar |
| `_legacy/_descartado-*.zip` | 20 MB | untracked, já ignorado | mover p/ fora do repo |
| `functions/node_modules`+`coverage`+`firebase-debug.log` | ~136 MB | local only (ok no git) | limpar local |

---

## 3. Integrações mapeadas (`functions/services/`)

- **Core:** Firebase — Firestore, Hosting, Functions, Auth, App Check.
- **Open Finance:** Pluggy.
- **Billing:** Asaas (default em `.env.production`) **+** Stripe.
  ⚠️ Stripe ainda em chaves de **teste** (`sk_test`) — confirmar provedor LIVE.
- **IA/LLM:** Gemini, Groq, OpenAI, Claude, DeepSeek (cascata de fallback).
- **Afiliados:** Lomadee + Monetizze.
  ⚠️ health-check aponta `monetizzeToken: missing` — webhook de cashback pode
  falhar em silêncio.
- **Comunicação:** WhatsApp (Meta Cloud), Telegram, Resend/Zoho (e-mail).
- **Mercado:** BRAPI (B3/cripto), BCB (Selic/IPCA/Tesouro).
- **Google Cloud:** Vision (OCR) + Speech (STT).
- **Mobile:** Capacitor 8 (android/ios nativos + biometria).

---

## 4. Checklist de Segurança & Compliance (CISO/DPO)

- ✅ Segredos **não versionados** — só chaves públicas `VITE_*` em
  `.env.production`; `functions/.env` no gitignore.
- ✅ App Check ativo; segredos LLM via proxy Cloud Function (correto).
- 🟡 **Stripe em modo teste** com Asaas como default — risco de cobrança
  quebrada se alguém acionar o fluxo Stripe em produção. Confirmar provedor.
- 🟡 `monetizzeToken: missing` — resolver ou desativar o caminho Monetizze.
- 🟡 Árvore suja em `main` — trabalho não commitado é risco operacional.
- ✅ Nenhuma limpeza foi destrutiva: tudo movido (reversível via git), nada
  deletado, nenhum deploy realizado.

---

## 5. Diff dos 26 arquivos não commitados

**Natureza:** passe único e coeso de **contraste/legibilidade** —
troca sistemática `text-si-5` → `text-si-4` em labels (subir contraste de
textos de baixa legibilidade), mais a extração de `Input/Select` de
`components/ui/Field` no `CreditHub.tsx`. Alinha com o tema do último commit
("off-brand focus and UI fixes"). Maiores diffs: `CreditHub.tsx` (165 linhas,
inclui refactor de campos), `Accounts.tsx` (46), `Profile.tsx` (20).

**Recomendação:** commitar como peça única, ex.:
`style(ui): sobe contraste de labels (si-5 → si-4) e usa Field no CreditHub`.
Nada solto ou perigoso identificado.

---

## 6. Próximos Passos (fundador)

**Git (rodar local, após destravar o índice):**

```bash
# 1. Remover o lock órfão
del .git\index.lock          # (ou: rm -f .git/index.lock)

# 2. Destrackar binários já ignorados
git rm -r --cached screenshots public/index_files

# 3. Commit da limpeza (separado do passe de UI)
git add .gitignore CLAUDE.md docs/
git commit -m "chore(repo): enxuga CLAUDE.md, arquiva docs historicos e destrack binarios"

# 4. Commit do passe de contraste (seus 26 arquivos)
git add src/
git commit -m "style(ui): sobe contraste de labels (si-5 -> si-4) e Field no CreditHub"
```

**Decisões pendentes (suas):**
1. Confirmar provedor de billing LIVE (Asaas vs Stripe) e migrar Stripe de
   test → live se for o caso.
2. Resolver `monetizzeToken` (configurar secret ou desativar o caminho).
3. Decidir destino das 3 branches antigas (merge ou apagar).
4. Mover `_legacy/_descartado-2026-06-17.zip` (20 MB) para fora do repo.

**Manutenção contínua:** manter o CLAUDE.md enxuto e jogar histórico de sessão
no `docs/CHANGELOG.md` (regra de governança). Considerar rodar esta auditoria
de higiene a cada 1–2 meses.
