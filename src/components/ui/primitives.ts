/**
 * Primitivos do design system Pierre — ponto único de import.
 *   import { Button, Card, Badge, Input, Select } from '../components/ui/primitives';
 */
export { Button, buttonClasses, type ButtonProps, type ButtonVariant, type ButtonSize } from './Button';
export { Card, CardHeader, type CardProps, type CardPadding } from './Card';
export { Badge, type BadgeProps, type BadgeTone } from './Badge';
export { Field, Input, Select, type InputProps, type SelectProps } from './Field';
